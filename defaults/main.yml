---
# defaults file for OMGOTA
ota_timestamp: "{{ansible_date_time.iso8601}}"
wifi_ssid:
wifi_pass:
ota_pass:
mqtt_server:
mqtt_pass:
nodes:
  - name:
    ip:
    mac:
  - name:
    ip:
    mac:
options:

